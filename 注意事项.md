# 部署注意事项（Trae 创造力大赛作品收录 + 上线）

> 适用范围：本次新增/核对的三件 Trae 2026 创造力大赛作品，以及上线前的红线检查。
> 安全类待办（root 密码轮换、COS 防盗链）的完整条目见工作记忆，此处仅列动作项。

## 一、三件 Trae 作品状态与处理原则

统一采用「资料卡 + 外链」模式，**未克隆作者专有源码**（避免 X-Frame-Options 拦截与版权/密钥风险）：

| 作品 | 文件 | 赛道 / 阶段 | 处理方式 |
|---|---|---|---|
| 苍穹降临（3D 交通沙盘） | `cangqiong.html` + `trae-cangqiong-analysis-2026.html` | 三维可视化 | 已完整本地化：three.js 存于 `lib/three/`，离线可跑，无外链依赖 |
| 数学之美 · 3D 数学可视化 | `lab/products/math-beauty.html` | 学习工作 · 外部应用 | 参考卡 + 外链（iframe 被 `X-Frame-Options` 拦截，故不内嵌） |
| MetaMath-Agent（数学建模论文工作台） | `lab/products/metamath-agent.html` | 学习工作 · **初赛** | 参考卡 + 外链（Demo 为静态展示版，真实后端未公开） |

### 关键注意事项
1. **数学之美不可 iframe 内嵌**：作者部署域名返回 `X-Frame-Options`，直接 `<iframe>` 会白屏。必须保留「在线体验」外链按钮 + 全量文档资料卡。
2. **MetaMath-Agent Demo 是静态展示版**：仅展示核心流程 / 界面 / 产物，不含真实后端密钥与运行环境；体验请走外链。
3. **苍穹降临已自包含**：three.js 已下载到 `lib/three/`，`cangqiong.html` 引用本地路径，离线可用，无需外链。
4. 三件作品的原帖 / 报名帖 / 在线地址均已在对应资料卡中留痕，便于回溯。

## 二、三端一致性（PC / 移动 / 平板）

- 新资料卡（`math-beauty.html`、`metamath-agent.html`）均响应式：`<meta viewport>` + `.wrap{max-width:880px}` + `.grid` 在 ≤640px 折叠为单列，三端均可正常浏览。
- 上线后须在三端各开一次验证：
  ① 返回指挥台链接（`.back` → `../index.html`）正常；
  ② 外链按钮在新标签打开；
  ③ 窄屏网格不溢出、不出现横向滚动。

## 三、发布流水线红线（sanitize.mjs）

- 发布链路：私有仓 `push main` → Actions `publish-pages`（sanitize.mjs 脱敏）→ orphan 强推公开仓 `Epic-super.github.io`。
- **务必保持 `scripts/sanitize.mjs` 完整性（>100 行）**：多设备/多会话合并曾把它破坏成仅剩 DENY 数组，导致 Actions「成功但 cd dist 失败」、公开站静默停更。任何合并后先确认行数。
- `lab/content/`、`lab/编程案例/` 仍在 DENY 排除列表；本次新增两件作品在 `lab/products/`，属公开范围，不受影响。
- 若 Actions 报「缺少密钥 PAGES_PAT」= secret 失效，按 `_scratch/archive_20260826/update_secret.py` 加密流程重写。

## 四、安全待办（上线前后尽快处理，属高危）

1. **腾讯云 root 登录密码轮换**：曾于对话中明文暴露，必须轮换（控制台 → 账号中心 / 访问管理 → 安全设置 → 登录密码）。
2. **COS 桶 Referer 防盗链**：桶 `workbench-408-1443157086`（ap-shanghai，ACL=公有读）建议仅允许 `epic-super.github.io` 引用，降低付费 PDF 被盗链风险（COS 控制台 → 存储桶 → 安全设置 → Referer 防盗链）。

## 五、上线验证三连

发布后于公开站核对：
1. `index.html` 的 `app-version` meta 已更新；
2. 新功能锚点（`math-beauty` / `metamath-agent`）grep 命中；
3. 两件产品页 HTTP 200。
